[package]
name = "makepad-example-native-text-input"
version = "1.0.0"
edition = "2021"

[dependencies]
makepad-widgets = { path = "../../widgets", version = "2.0.0" }

[features]
default = []
diag-native-leak-count = ["makepad-widgets/diag-native-leak-count"]

[dev-dependencies]
makepad-test = { path = "../../libs/makepad_test", version = "0.1.0" }
